"use strict";

var decorateTypeDef = require("../decorateTypeDef"); // Use eval("require") so webpack doesn't bundle the `url` module


var URL = global.URL || eval("require")("url").URL;
module.exports = decorateTypeDef({
  description: "URL",
  serializedDescription: '{ $type: "URL", $value: string }',
  check: function check(val) {
    return val instanceof URL;
  },
  serialize: function serialize(url) {
    return {
      $type: "URL",
      $value: url.href
    };
  },
  checkSerialized: function checkSerialized(serialized) {
    return serialized.$type === "URL";
  },
  deserialize: function deserialize(serialized) {
    return new URL(serialized.$value);
  }
});