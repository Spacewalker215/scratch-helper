"use strict";

var prettyFormat = require("pretty-format");

function assertType(value, typeDef) {
  if (!typeDef.check(value)) {
    throw assertType.makeError(typeDef.description, value);
  }
}

assertType.makeError = function makeError(description, value) {
  return new TypeError("Expected ".concat(description, ", but received ").concat(prettyFormat(value)));
};

module.exports = assertType;